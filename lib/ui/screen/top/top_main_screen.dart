import 'package:flutter/material.dart';

class Top_mainScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('sdcs')),
        body: Center(
          child: RaisedButton(
            onPressed: () {},
          ),
        ));
  }
}
